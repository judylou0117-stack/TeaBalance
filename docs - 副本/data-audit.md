# Tea Balance 数据审计

本报告仅记录导入前后的结构与来源问题，不修改任何原始 Excel，也不补造医疗规则。

## 原始资料

- `GBT46939-2025附录A中医体质分类判定题目.xlsx`：已复制到 `resources/raw/`。
- `knowledge base.xlsx`：已复制到 `resources/raw/`。
- `安全规则表.xlsx`：已复制到 `resources/raw/`。
- `判定规则表.xlsx`：已复制到 `resources/raw/`。
- `评分映射表.xlsx`：已复制到 `resources/raw/`。
- `中医养生茶饮明细 2.xlsx`：已复制到 `resources/raw/`。
- 导入完成后的复核中，外部目录内有 5 个原表正被其他程序占用，无法再次打开计算文件哈希；导入程序已经成功读取并复制其内容，且本项目没有对外部原表执行写入操作。

## 数据条数

- 题目：27 条。
- 评分映射：30 条。
- 判定规则：6 条。
- 茶饮：16 条。
- 安全规则：14 条，另有 3 条无编号备注。
- 体质知识：9 条双语记录。

## 题目、体质编码与性别分支

- 题目编号为 `Q001` 至 `Q027`；评分映射引用的题目均存在。
- 映射中出现的体质编码为 A, B, C, D, E, F, G, H, I；与统一编码 A–I 一致。
- Q017 的题目表性别分支为 `female`，Q018 为 `male`；两题都出现在湿热质（F）映射中。导入后的评分模块只会按用户声明的性别选择其中一题。未透露性别时不会代填，并将湿热质严格判定标为信息不足。

## 空字段、未命名列与重复数据

- 重复文件：未发现内容哈希相同的原始文件。
- **检查项**：已跳过 Excel 临时锁定文件：~$GBT46939-2025附录A中医体质分类判定题目.xlsx、~$中医养生茶饮明细 2.xlsx、~$判定规则表.xlsx、~$安全规则表.xlsx、~$评分映射表.xlsx
- **检查项**：knowledge base.xlsx 第 9 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 10 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 11 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 12 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 13 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 14 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 15 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 16 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 17 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 18 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 19 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 20 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 21 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 22 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 23 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 24 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 25 列无字段名，数据行中有 0 个非空值。
- **检查项**：knowledge base.xlsx 第 26 列无字段名，数据行中有 0 个非空值。
- **检查项**：安全规则表.xlsx 第 9 列无字段名，数据行中有 2 个非空值。
- **检查项**：判定规则表.xlsx 第 5 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 6 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 7 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 8 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 9 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 10 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 11 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 12 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 13 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 14 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 15 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 16 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 17 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 18 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 19 列无字段名，数据行中有 0 个非空值。
- **检查项**：判定规则表.xlsx 第 20 列无字段名，数据行中有 0 个非空值。
- `knowledge base.xlsx` 在中文九条记录后有两行空分隔行，随后为英文表头与九条英文记录；导入程序按编号将中英文配对。
- `判定规则表.xlsx` 的第 5–20 列为空列；导入仅使用有字段名的前四列。
- `安全规则表.xlsx` 第 9 列没有字段名，但部分行存放网址；导入程序保留为 `sourceUrl`，并在此报告标注。

## 来源、审核与安全规则

- 茶饮来源缺失：无。
- 茶饮审核状态缺失：无。
- **问题**：安全规则编号序列缺少：S009（未补造 S009）。
- **问题**：安全规则来源为 `/` 或为空：S001, S002, S015。
- **检查项**：安全规则网址缺失：S001, S002, S005, S006, S007, S008, S010, S011, S012, S013, S014, S015。
- S014 的风险级别写为“警告/阻断”，无法从表格唯一确定动作。规则模块会保留该原文，并按“需人工审核的警告”处理，不擅自升级为阻断。
- 表格没有“咖啡因敏感”这一独立触发条件；仅在孕哺与儿童规则中列出含咖啡因（茶）材料。规则模块会返回明确的数据缺口警告，不将其伪装为国标医学结论。

## 需要人工审核的自动推荐边界

- 以下茶饮含安全规则无编号备注列出的“需要谨慎使用的材料”，在涉及该类材料但规则表没有精确条件—动作对应时，不能仅依靠自动匹配作最终结论：2（益气养生茶）、6（滋阴润养茶）、7（麦冬石斛茶）、8（轻身化湿茶）、12（活血通络茶）、13（山楂红糖水）、15（固表抗敏茶）。
- 安全表列出的材料、药物和疾病情形只是示例，表格备注明确说明未覆盖其他材料；规则后端只执行已列出的范围。
- 所有“明显、持续或严重不适”的处理均在代码中标记为产品安全边界，不归因于国标题目或安全表。
