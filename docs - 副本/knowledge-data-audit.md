# Tea Balance 知识检索数据检查报告

本报告记录知识资料的导入、关联与来源字段检查。它不修改原始 Excel，不补写医学依据，也不能替代对来源的人工核实。

## 原始资料

- `体质知识表.xlsx`：已复制到 `resources/raw/`。
- `RAG茶饮知识表_Sheet2.xlsx`：已复制到 `resources/raw/`。
- `体质日常表述.xlsx`：已复制到 `resources/raw/`。

## 导入结果

- 体质知识：9 条；跳过空行 1 条。
- 茶饮知识：16 条；跳过空行 0 条。
- 日常表达：45 条；跳过空行 0 条。
- 茶饮表中的 `URL` 已映射为生成 JSON 的 `source_url`；体质知识表未提供 URL，保留其 `source_name` 和 `source_section`。

## 字段、编号与体质编码

- 未发现重复的 knowledge_id。
- 知识记录的必填字段完整。
- 茶饮知识记录均包含 tea_id。
- 体质知识的体质编码均为 A–I。
- 茶饮知识的体质编码均为 A–I。
- 日常表达的体质编码均为 A–I。

## 与规则后端茶饮的关联

- 已找到 16 条 tea_id 与规则茶饮编号的关联。规则编号为数字形式时，导入检查会将 `T001` 与 `1` 视为同一编号。
- 所有茶饮知识均能关联到规则后端茶饮。
- 关联茶饮的名称与规则茶饮名称一致。
- 关联茶饮的配料字段与规则茶饮配料在忽略剂量、单位和标点后保持一致。

## 审核状态与来源边界

- 默认只有 `review_status` 为 `verified` 的资料会进入正式检索。未通过该筛选的资料仍保留在生成 JSON 中，便于后续人工复核。
- `verified` 是组员在资料中填写的审核状态；程序只检查字段值，不能代替对来源、内容或适用边界的人工核实。
- 当前所有知识记录均标记为 verified。
- 所有茶饮知识记录均包含来源名称和 URL。

## 检索边界

- 日常表达仅用于扩展检索词和说明匹配原因，不会参与国标体质评分，也不会直接判定体质。
- 茶饮知识只会从规则后端安全过滤后仍可显示的茶饮编号中检索；被阻断的茶饮不会被知识检索重新加入。
